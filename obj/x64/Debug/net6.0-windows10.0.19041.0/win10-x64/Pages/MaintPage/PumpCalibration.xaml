<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ASDS_dev.Pages.MaintPage.PumpCalibration"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ASDS_dev.Pages.MaintPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:ASDS_dev.Controls"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Main Content -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header Control -->
        <controls:HeaderControl Grid.Row="0" />

        <!-- Main content placeholder -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
               
                <RowDefinition Height="*"/>
                
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="#414854" Padding="3" Margin="3" >
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   Foreground="#4385f0"
                   FontSize="20"
                   Padding="10"
                   FontWeight="Bold"
                   Text="Pump Calibration" />

                    <Grid Width="370" Height="30" Margin="80,0,0,0" HorizontalAlignment="Left">
                        <!-- Background Rectangle -->
                        <Rectangle Fill="#5f327d" RadiusX="4" RadiusY="4"/>

                        <!-- Text on top -->
                        <TextBlock Text="System Ready For Dispensing Solution"
               Foreground="White"
               FontSize="15"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               FontWeight="Bold"/>
                    </Grid>

                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Background="#414854" Padding="3" Margin="3,0,3,3" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <TextBlock  FontSize="15" Foreground="#4385f0">Divosan</TextBlock>
                   
                </Grid>

            </Grid>

        </Grid>

        <!-- Footer Control -->
        <controls:FooterControl Grid.Row="2" />
    </Grid>
</Page>

