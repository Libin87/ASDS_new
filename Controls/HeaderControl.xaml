<?xml version="1.0" encoding="utf-8"?>
<UserControl 
    x:Class="ASDS_dev.Controls.HeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ASDS_dev"
    xmlns:vm="using:ASDS_dev.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <UserControl.DataContext>
        <vm:HeaderViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.Resources>
            <local:DateTimeToStringConverter x:Key="DateTimeConverter"/>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="2">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#ae72c4" Offset="0" />
                    <GradientStop Color="#44035c" Offset="1" />
                </LinearGradientBrush>
            </Grid.Background>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical">
                <TextBlock Grid.Column="0" Text="Dr. Reddy s" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <TextBlock Grid.Column="1" Text="Automated Solution Dispensing System [ASDS V4.0]" FontSize="16" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{Binding CurrentTime, Converter={StaticResource DateTimeConverter}}" FontSize="14" Foreground="White" Margin="0,0,40,0"/>
                    <TextBlock x:Name="UsernameText" Text="User: " Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Margin="0,0,5,0" Padding="5" CornerRadius="5" Width="100" x:Name="LoginButton" Click="LoginButton_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Viewbox Width="20" Height="20" Margin="0,0,5,0">
                            <Canvas Width="24" Height="24">
                                <Path Fill="White" Data="M12,4 A4,4 0 0 1 16,8 A4,4 0 0 1 12,12 A4,4 0 0 1 8,8 A4,4 0 0 1 12,4 M12,14 C16,14 19,17 19,21 L5,21 C5,17 8,14 12,14 Z"/>
                            </Canvas>
                        </Viewbox>
                        <TextBlock Text="Login" VerticalAlignment="Center" Foreground="White"/>
                    </StackPanel>
                </Button>
                <Button Padding="5" CornerRadius="5" Margin="5,0,5,0" Width="100" x:Name="LogoutButton"  Click="LogoutButton_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Viewbox Width="20" Height="20" Margin="0,0,5,0">
                            <Canvas Width="24" Height="24">
                                <Path Fill="White" Data="M10,17 L15,12 L10,7 V10 H2 V14 H10 V17 Z M18,20 H6 V4 H18 V20 Z"/>
                            </Canvas>
                        </Viewbox>
                        <TextBlock Text="Logout" VerticalAlignment="Center" Foreground="White"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
